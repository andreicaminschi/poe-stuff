/** Shown while the catalog and the config load. */
export function BootScreen() {
  return (
    <div className="boot">
      <p className="label">Loading the published catalog</p>
    </div>
  );
}
