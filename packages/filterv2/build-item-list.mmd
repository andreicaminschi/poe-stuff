---
title: buildItemList — every item the game can show, named and flagged
---
flowchart TB
  CLI["build-item-list-cli.ts<br/>league, hour two back, --out, --cache-dir"]
  GGG["createGGGService — one instance for the whole run<br/>one service is one IP, and GGG counts a process against one budget"]
  REPOE["repoe.getBaseItems — base_items.json<br/>cached under cache/repoe, keyed by the hour"]

  CLI --> GGG
  CLI --> REPOE

  COLLECT["<b>collectItems</b> — /data/items for everything but currency,<br/>the Currency Exchange for currency, RePoE to name the paths<br/><i>drawn in build-item-list/collect-items.mmd</i>"]
  FILL["fillFromRepoe — itemClass, releaseState, tags<br/><b>last, and it wins</b>: the game's own export outranks<br/>whatever the trade site claimed"]

  GGG --> COLLECT
  REPOE --> COLLECT
  COLLECT --> FILL
  REPOE --> FILL

  OUT[("data/items.json<br/>items keyed by name — or by metadata leaf<br/>where nothing can name the row yet<br/>plus repoeIncomplete")]
  FILL --> OUT

  GONE>"<b>A fourth source was removed.</b> The league's Item Filter Information<br/>forum post was read through @poe/ggg and extracted with claude -p.<br/>It ran beside RePoE here, fed detectNewLeague, and applied this<br/>league's renames between collectItems and fillFromRepoe.<br/>The forum endpoints are gone, so a league launch is now invisible."]
  GONE -.- COLLECT

  classDef note fill:#f8fafc,stroke:#94a3b8,color:#334155
  class GONE note

  click COLLECT href "build-item-list/collect-items.mmd" "collectItems in full"
